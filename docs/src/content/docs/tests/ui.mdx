---
title: UI
---

import { Tabs, TabItem } from '@astrojs/starlight/components'
import PackageManagers from '../../../components/PackageManagers.astro'

export const pkg = 'astro'

Two consecutive commands using `starlight-package-managers`:

<div data-testid="tabs-icons">
  <PackageManagers pkg={pkg} />
</div>

<PackageManagers pkg={pkg} />

Some text in between.

<PackageManagers pkg={pkg} />

Some more text.

Some code followed by a command:

```ts
export const pkg = 'astro'
```

<PackageManagers pkg={pkg} />

A command with only 1 package manager:

<div data-testid="tabs-single">
  <PackageManagers pkg={pkg} pkgManagers={['pnpm']} />
</div>

A command with no icons:

<div data-testid="tabs-no-icons">
  <PackageManagers pkg={pkg} icons={false} />
</div>

A compact command:

<div data-testid="compact-icons">
  <PackageManagers pkg={pkg} variant="compact" />
</div>

A compact command with only 1 package manager:

<div data-testid="compact-single">
  <PackageManagers pkg={pkg} pkgManagers={['pnpm']} variant="compact" />
</div>

A compact command with no icons:

<div data-testid="compact-no-icons">
  <PackageManagers pkg={pkg} icons={false} variant="compact" />
</div>
