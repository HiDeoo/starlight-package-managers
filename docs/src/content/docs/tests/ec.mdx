---
title: Expressive Code
---

import { Tabs, TabItem } from '@astrojs/starlight/components'
import PackageManagers from '../../../components/PackageManagers.astro'

export const pkg = 'astro'

The default commands:

<PackageManagers pkg={pkg} />

The commands with no frame:

<PackageManagers pkg={pkg} frame="none" />

The commands with a terminal frame containing a title:

<PackageManagers pkg={pkg} title="Installing dependencies" />
